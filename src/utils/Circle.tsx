import { Box } from "@mui/material";

const Circle = () => {
	return (
		<Box
			sx={{
				width: "20px",
				height: "20px",
				backgroundColor: "#3461FF",
				borderRadius: "50%",
				display: "inline-block",
			}}
		/>
	);
};

export default Circle;
